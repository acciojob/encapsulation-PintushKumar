package com.driver;

public class Main {
    RWOnly rwonly = new RWOnly();
    // rwonly.name = "Pintush";
    //Facing an error - name' has private access in 'com.driver.RWOnly';

    //rwonly.setName("Pintush");
    //System.out.println(rwonly.getName());
}